import React from "react";
import { useParams } from "react-router-dom";

export default function ClassInvite() {
  const { class_code } = useParams();
  const checkClassInvite = () => {};

  return <div>ClassInvite</div>;
}
